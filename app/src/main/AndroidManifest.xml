<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hp.dddd">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- if you want to load images from a file OR from the internet   MainActivity    MyLocationActivity -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoo"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MyLocationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_user_sign_in" />
        <activity android:name=".Activity_user_sign_in1" />
        <activity android:name=".Activity_singout" />
        <activity android:name=".ActivitySingin" />
        <activity android:name=".SplAS" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".tst" />
        <activity android:name=".MapsActivityda" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MapsActivityt"
            android:label="@string/title_activity_maps_activityt" />
        <activity android:name=".log_in" />
        <activity android:name=".MainActivityd" />
        <activity
            android:name=".MapsActivity_sabt_restoorann"
            android:label="@string/title_activity_maps_activity_sabt_restooran" />
        <activity android:name=".useer_restooran" />
        <activity android:name=".MapsActivity_sabt_restooran" />
        <activity
            android:name=".user_reetooran1"
            android:label="@string/title_activity_user_reetooran1" />
        <activity android:name=".sabt_ratooran_b_n" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>
